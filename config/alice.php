<?php

return [

    // default locale to use with faker if none is specified in the expression
    'locale' => 'en_US',

    // a seed  to make sure faker generates data consistently across runs, set to null to disable
    'seed' => 1,

    // custom faker providers in addition to the default ones from faker
    'providers' => [

    ]
];